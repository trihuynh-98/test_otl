apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tempo
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/<YOUR_GITHUB_USER>/tracing-stack.git"
    targetRevision: main
    path: base/tempo
    helm:
      chart: tempo
      repoURL: "https://grafana.github.io/helm-charts"
      version: "1.23.0"  # example version
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: tracing
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
